# AWS Docs Assistant — EC2 / production (instance profile for AWS APIs).
#
#   docker compose up -d --build
#   docker compose logs -f backend
#   docker compose --profile ingestion run --rm ingestion --force-reprocess
#
# Local Mac with ~/.aws credentials:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - ./backend/.env
    environment:
      AWS_REGION: ap-south-1
    volumes:
      - huggingface_cache:/app/.cache/huggingface
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 300s
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    env_file:
      - ./frontend/.env
    environment:
      BACKEND_API_URL: http://backend:8000
      API_BASE_URL: http://backend:8000
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

  ingestion:
    profiles: ["ingestion"]
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - ./backend/.env
    environment:
      AWS_REGION: ap-south-1
    volumes:
      - huggingface_cache:/app/.cache/huggingface
    working_dir: /app
    command: ["python", "run_ingestion.py", "--max-documents", "1"]

volumes:
  huggingface_cache:
