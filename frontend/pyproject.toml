[project]
name = "agentic-aws-docs-assistant-frontend"
version = "0.1.0"
description = "Streamlit UI"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.0.0",
    "streamlit>=1.41.0",
]

[tool.uv]
package = false
